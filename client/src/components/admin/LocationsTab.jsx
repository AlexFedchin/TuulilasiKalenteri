import React from "react";
import { Typography } from "@mui/material";

const LocationsTab = () => {
  return (
    <div>
      <Typography variant="h5">Locations</Typography>
      <Typography variant="body1">Manage locations here.</Typography>
    </div>
  );
};

export default LocationsTab;
